name: Site integrity

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-site-integrity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check generated site files
        run: python scripts/check_site_integrity.py
